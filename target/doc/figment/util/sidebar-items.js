window.SIDEBAR_ITEMS = {"fn":[["bool_from_str_or_int","A helper to deserialize `0/false` as `false` and `1/true` as `true`."],["diff_paths","A helper function to determine the relative path to `path` from `base`."],["nest","Given a key path `key` of the form `a.b.c`, creates nested dictionaries for for every path component delimited by `.` in the path string (3 in `a.b.c`), each a parent of the next, and the leaf mapping to `value` (`a` -> `b` -> `c` -> `value`)."]],"mod":[["vec_tuple_map","A helper to serialize and deserialize a map as a vector of `(key, value)` pairs."]]};